[package]
name = "libaaf-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reader for AAF compositions, built on libaaf (GPL-2.0-or-later)"
links = "aaf"
build = "build.rs"

[dependencies]
thiserror.workspace = true

[build-dependencies]
cc = "1"
cmake = "0.1"
